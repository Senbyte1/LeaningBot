const Discord = require("discord.js");

module.exports = {
    name: "leaning",
    run: async (client, message, args, print, prefix) => {
		  message.channel.send("<@590286037323087882> Ping!\n LeaningDown is a furry!");
    }
}